#ifndef IMAGE_FILTER_H // 헤더가드. 라이브러리 중복 포함 방지
#define IMAGE_FILTER_H

#include <iostream>
#include <../src/logger/logger.h>


class imageFilter {
    public:
        imageFilter();
        ~imageFilter();
        void printImageFilter();
};


#endif // IMAGE_FILTER_H

// void printImageFilter();
