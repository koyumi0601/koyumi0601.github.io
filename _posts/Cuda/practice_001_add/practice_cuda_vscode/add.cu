// add.cu
// 
// #include<memory.h>
#include <cuda.h>

// __global__ void addKernel(float *a, float *b, float *c, int n) {
//     int idx = blockIdx.x * blockDim.x + threadIdx.x;
//     if (idx < n) {
//         c[idx] = a[idx] + b[idx];
//     }
// }

